#!/bin/sh

layout="$(yaxwm -c print layout)"

case $layout in
       tile) i='' ;;
       mono) i='' ;;
       grid) i='' ;;
     spiral) i='' ;;
    dwindle) i='' ;;
       none) i=''
esac
printf "%s\n" "$i"
